import { InitialStructure } from './initial-structure.model';

describe('InitialStructure', () => {
  it('should create an instance', () => {
    expect(new InitialStructure()).toBeTruthy();
  });
});
